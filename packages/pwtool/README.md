# pwtool

`pwtool` monitors PipeWire default sink/source and prints status output for bars.

Usage:

```bash
pwtool [--i3statusrs] [--debug] <sink|source>
```

Examples:

```bash
pwtool sink
pwtool --i3statusrs source
```
